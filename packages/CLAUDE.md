# packages/

Workspace packages published under the `@invinite-org/chartlang-*` npm scope.

## Conventions

- Every subdirectory matches the §22.4 template: `package.json`,
  `tsconfig.json`, `vitest.config.ts`, `README.md`, `src/index.ts`,
  `src/index.test.ts`.
- Templates are generated by `pnpm scaffold` (see `scripts/scaffold.ts`).
  Adding a new package = append to `PACKAGE_DIRS` in the scaffold and
  re-run. Do not write the six template files by hand.
- `src/index.ts` must export a `PACKAGE_VERSION` constant until the
  package ships real exports (Phase 1+). Two-line MIT header at the top
  of every `.ts` file.
- `vitest.config.ts` enforces 100% coverage thresholds. Coverage excludes
  `index.ts` (barrel) and `types.ts` (declarations only).
- READMEs cap at 100 lines and follow §17.1 structure (title, stability
  label, purpose, install, public surface, MV API, docs link, license).
