//@version=6
indicator("Linefill two-line", overlay=true)
var line a = line.new(bar_index, high, bar_index, high)
var line b = line.new(bar_index, low, bar_index, low)
var linefill fill = linefill.new(a, b, color.new(color.teal, 80))
