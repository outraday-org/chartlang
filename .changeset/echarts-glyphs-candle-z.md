---
---

echarts example adapter: render the five glyph plot kinds
(shape/character/arrow/marker/label) as native `graphic` elements with real
geometry, colour `candle-override` bodies by bar direction (bull/bear/doji),
and honour drawing/plot `z` via a shared-sort-driven `zlevel` band. Private
example package — no published version bump.
