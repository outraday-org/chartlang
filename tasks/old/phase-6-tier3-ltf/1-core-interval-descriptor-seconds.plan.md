# Task 1 Plan

## Context

Implemented `IntervalDescriptor.intervalSeconds?` and `intervalToSeconds`.

## Steps

1. Widened `packages/core/src/types.ts`.
2. Added `packages/core/src/interval/intervalToSeconds.ts`.
3. Re-exported helper from `packages/core/src/index.ts`.
4. Added unit and property tests.

## Gates

- `pnpm typecheck`
- focused core tests

