# Task 4 — Fixtures, compile round-trip, docs/skills, acceptance

> **Status: TODO**

## Goal

Prove the end-to-end T12 path: converter fixtures (`var color`, bool/string
`var` history) that pass the compile round-trip, an author-facing example/docs
entry for the new `state.*` surface, the regenerated skill/primitives
reference, and the MASM acceptance slice. This task lands **no new behavior** —
it locks Tasks 1–3 behind fixtures, docs, and the gates.

## Prerequisites

Task 1 (core), Task 2 (runtime), Task 3 (converter lowering).

## Current Behavior

- No converter fixture exercises a `var color` slot or bool/string `var`
  history (grep `packages/pine-converter/fixtures/*.pine`).
- `packages/pine-converter/src/tests/fixtures-compile.test.ts` compiles every
  clean fixture through `@invinite-org/chartlang-compiler`; its
  `KNOWN_NON_COMPILING` set parks the bool/string-history gap today.
- The author skill reference
  (`skills/chartlang-coding/references/primitives.md`) is generated by
  `pnpm skills:generate` and gated by `skills:gate`; it lists the `state.*`
  surface and must include the new members.

## Desired Behavior

- Fixtures covering each new lowering convert AND compile clean (not parked in
  `KNOWN_NON_COMPILING`).
- The new `state.color` / `state.boolSeries` / `state.stringSeries` appear in
  the generated primitives reference + docs.
- The MASM color/position-state slice converts to compiling chartlang.

## Requirements

### 1. Converter fixture triples (`packages/pine-converter/fixtures/`)

Add `NN-name.pine` + `.expected.chart.ts` + `.expected.diagnostics.json` for
(assign the next free `NN` via `ls fixtures` — do not hardcode a guessed
number):

- **`var-color-slot`** — `var color c = na` written each bar from a ternary →
  `state.color`; assert no `scalar-state-type-defaulted`.
- **`var-bool-history`** — `var bool active = false` with `active[1]` /
  `active[2]` reads → `state.boolSeries`; assert no `series-history-non-numeric`.
- **`var-string-history`** — `var string label = "-"` with `label[1]` →
  `state.stringSeries`.
- **`masm-position-state`** — a trimmed MASM slice combining a `var color`
  exit-color and `long_pos_active[1]` bool history (the acceptance script).

### 2. Compile round-trip (`src/tests/fixtures-compile.test.ts`)

The new fixtures must compile clean. **Remove** any bool/string-history entry
this unblocks from `KNOWN_NON_COMPILING`; do not add new ones.

### 3. Core example + docs

- An author example (e.g. `examples/scripts/persistent-color.chart.ts` or the
  repo's example convention) using `state.color` + `state.boolSeries`, with the
  example-gate metadata.
- Docs page / reference entry for the new `state.*` members
  (`docs/` per the `state` namespace doc convention).

### 4. Skill / primitives reference

Run `pnpm skills:generate` so `skills/chartlang-coding/references/primitives.md`
re-emits with `state.color` / `state.boolSeries` / `state.stringSeries`. The
`skills:gate` must pass (CLAUDE.md repo rule: a `state.*` surface change updates
the author skill in the same PR).

### 5. CLAUDE.md updates

- `packages/core/src/state/` + `packages/runtime/` CLAUDE notes for the new
  slots (per-folder invariants rule).
- `packages/pine-converter/CLAUDE.md` §"KNOWN GAPS" — confirm the bool/string
  history deferral is retired (the Task 3 change is reflected).

## Edge cases

- Diagnostics JSON must be exact (the fixtures assert the absence of
  `series-history-non-numeric` / `scalar-state-type-defaulted` for these
  cases) — regenerate, don't hand-edit.
- `varip color` / `varip` non-numeric series fixtures stay in the deferred
  bucket (mirror `varip-series-approximated`) — out of scope.

## Files to Create / Modify

| File | Action | Purpose |
|------|--------|---------|
| `packages/pine-converter/fixtures/NN-var-color-slot.*` | Create | Color-slot fixture triple. |
| `packages/pine-converter/fixtures/NN-var-bool-history.*` | Create | Bool-history fixture triple. |
| `packages/pine-converter/fixtures/NN-var-string-history.*` | Create | String-history fixture triple. |
| `packages/pine-converter/fixtures/NN-masm-position-state.*` | Create | MASM acceptance slice. |
| `packages/pine-converter/src/tests/fixtures-compile.test.ts` | Modify | Remove unblocked `KNOWN_NON_COMPILING` entries. |
| `examples/scripts/*.chart.ts` | Create | Author example. |
| `docs/**` + `skills/chartlang-coding/references/primitives.md` | Modify (regen) | Docs + skill reference. |
| `packages/core/src/state/CLAUDE.md`, `packages/runtime/CLAUDE.md`, `packages/pine-converter/CLAUDE.md` | Modify | Invariant notes. |

## Gates

- `pnpm typecheck`, `pnpm lint`
- `pnpm test` (converter fixture compile round-trip green; core/runtime 100%)
- `pnpm docs:check`
- `pnpm skills:generate` + `skills:gate`

## Changeset

Covered by Task 1's shared T12 changeset (no new changeset).

## Acceptance Criteria

- All four fixtures convert AND compile clean; none parked in
  `KNOWN_NON_COMPILING`.
- `state.color`/`boolSeries`/`stringSeries` documented + in the generated
  primitives reference; `skills:gate` green.
- MASM's `var color` exit-colors persist and `long_pos_active[1]` /
  `short_pos_active[1]` convert to compiling `state.boolSeries` reads.
