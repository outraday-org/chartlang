# Plan — Task 13: multi-adapter conformance runner + skills + docs index

## Scope (scripts/docs/skills only — NO package src, NO changeset)

### 1. `scripts/run-conformance.ts`
- Replace single canvas2d import with a 5-entry ADAPTER registry
  (canvas2d, lightweight-charts, uplot, echarts, konva), each tried
  `src/index.ts` then `dist/index.js`.
- Loop: import → if missing, `console.error("skip <name>: not built")`
  + continue (no hard fail); else run `runConformanceSuite(mod.default)`,
  log `<name>: N passed, M failed`, OR-accumulate failures.
- Preserve `--report` / `--check` semantics: write/check a report pair
  PER adapter (generalise `canvas2dReportPaths` → `adapterReportPaths(root, dir)`).
- Exit non-zero iff any adapter has scenario failures OR (under --check)
  any committed-report drift. Keep printing every failure.

### 2. `scripts/CLAUDE.md`
- Replace the single-canvas2d runner description with the multi-adapter
  iteration note in the Map row + a Conventions bullet.

### 3. `skills/chartlang-setup/references/adapter.md` (hand-written)
- Add a "Shared geometry layer" section: `decomposeDrawing` + `DrawPrimitive`
  IR (root export) and `paintPrimitive` / `RenderCtx` / `MockCanvasContext`
  (`/canvas` sub-path); the "decompose once, map primitives" pattern.
- Add a "Reference adapters" section listing all five examples + the two
  integration strategies (ctx-based: canvas2d/LC/uplot reuse `paintPrimitive`;
  scene/option mappers: konva→nodes, echarts→graphic).

### 4. `docs/adapters/index.md` (create)
- Overview index linking the five `reference/<id>` pages + a one-line
  geometry-layer mention.

### 5. `docs/adapters/reference/canvas2d.md` (create)
- Per-library page for the reference adapter, mirroring the existing
  uplot/lwc reference pages.

### 6. `docs/.vitepress/config.ts`
- Add an "Overview" item (`/adapters/`) and the four new + canvas2d entries
  to the `/adapters/` "Reference" sub-group.

### 7. `docs/getting-started/write-your-first-adapter.md`
- Add a short geometry-layer paragraph + point at the five examples.

### 8. `README.md`
- Add the five adapters (table) + one-line shared-geometry mention.
  Keep <= 300 lines (currently 211).

## Gates
- `pnpm conformance` (all five green via new runner)
- `pnpm docs:check`, `pnpm readme:check`
- `pnpm skills:gate` (primitives.md untouched — stays green)
- typecheck/lint on touched scripts; `pnpm docs:build` if practical.
