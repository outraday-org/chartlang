# Task 15 — Plan: generated docs gallery + README standardization + UX

## Scope (from the task file)

1. Extend `scripts/gen-adapters.ts` to ALSO emit `docs/adapters/gallery.md`
   from `ADAPTERS` (the registry SSOT). Header
   `<!-- GENERATED by pnpm adapters:generate — DO NOT EDIT -->`. Sections:
   intro, comparison matrix, per-adapter cards, "which should I choose?",
   FAQ/troubleshooting.
2. Extend `adapters:gate` to byte-diff BOTH the CLI bundle AND
   `gallery.md`. Keep the Task-14 bundle diff intact (bundle bytes
   unchanged — only ADD the gallery to the diff set).
3. VitePress: add Gallery at TOP of the `/adapters/` sidebar group + point
   the navbar "Adapters" entry at `/adapters/gallery`. Update
   `docs/CLAUDE.md` to list `gallery.md` among auto-generated pages.
4. Standardize the five `examples/*-adapter/README.md` to one template
   (≤100 lines, hand-authored — NOT generated).
5. Root README "Adapters" section → one-command install + gallery link.
   `docs/getting-started/write-your-first-adapter.md` → `add-adapter`
   fast path + gallery link.

## Key facts validated against the codebase

- Registry SSOT: `scripts/adapters/registry.ts` — `ADAPTERS` (5 entries:
  canvas2d, echarts, konva, lightweight-charts, uplot in that order),
  `AdapterRegistryEntry` (id, exampleDir, displayName, library,
  libraryRange, license, renderTech, strategy, fullSurface, approxBundleKb,
  bestFor), `githubFolder(entry)` →
  `https://github.com/outraday-org/chartlang/tree/main/<exampleDir>`,
  `REPO_URL`.
- Generator: `gen-adapters.ts` currently writes only into
  `packages/cli/src/generated/adapters/` (4 .ts files). `generateAdapters`
  + `renderAllOutputs` are the seams. The gate (`--check`) diffs that dir.
  I will add a SEPARATE gallery output (different OUT path) + fold it into
  the same `generateAdapters`/`--check` flow so ONE command emits both and
  ONE gate diffs both. Bundle output untouched.
- GitHub deep-link convention: folders `tree/main/<dir>`, files
  `blob/main/<dir>/<file>`.
- Conformance reports: ONLY `examples/canvas2d-adapter/CONFORMANCE.md`
  (+ `conformance-report.json`) is committed. The other four run pass/fail
  only (scripts/CLAUDE.md `run-conformance` note). So the per-card
  "conformance" deep link → canvas2d's committed `CONFORMANCE.md` when the
  adapter is canvas2d; for the other four, link the docs
  `/adapters/conformance` page (resolvable; no committed per-adapter file
  exists). FAQ documents that only canvas2d ships a committed report.
- `readme-check.ts` only structurally gates `examples/canvas2d-adapter`
  (the other four example READMEs are NOT in its dir list) but the line
  ≤100 cap is required by the task for all five. Current line counts:
  canvas2d 100, lwc 85, uplot 97, echarts 100, konva 80 — all ≤100. New
  template must keep all five ≤100. canvas2d MUST keep: stability label,
  "Not published" install line, "Public surface" heading, ```ts block,
  MIT line (readme-check requires these).
- `approxBundleKb` is hand-maintained (registry comment + task edge case)
  → documented in the gallery FAQ.

## Implementation steps

### A. Generator (`scripts/gen-adapters.ts` + test)

- Add `DOCS_DIR = join(REPO_ROOT, "docs/adapters")` and
  `GALLERY_PATH = join(DOCS_DIR, "gallery.md")`.
- Add `GALLERY_HEADER =
  "<!-- GENERATED by pnpm adapters:generate — DO NOT EDIT -->"`.
- Add pure `renderGallery(entries): string` (deterministic, no IO) →
  intro + matrix + cards + decision guide + FAQ. Uses `githubFolder` +
  `REPO_URL` for links. Per-card conformance link: canvas2d →
  `blob/main/examples/canvas2d-adapter/CONFORMANCE.md`; others →
  `/adapters/conformance` (relative docs link `./conformance`).
  Actually for a deep GitHub link, the task says "committed conformance
  report" — only canvas2d has one. Use canvas2d's committed report URL for
  canvas2d, and the canvas2d reference report for others is misleading, so
  others link the docs conformance page. Document in FAQ.
- In `generateAdapters`: after the bundle outputs, also
  write/diff the gallery. On `check`, add a gallery drift entry to the same
  `drift[]` list so the gate fails on a hand-edit.
- Test additions in `gen-adapters.test.ts`:
  - `renderGallery` is deterministic (a===b).
  - contains the GENERATED header, every adapter displayName + id +
    install command + githubFolder, the matrix header row, the
    approxBundleKb FAQ note.
  - the committed `docs/adapters/gallery.md` equals `renderGallery(ADAPTERS)`
    byte-for-byte.
  - `generateAdapters({check:true})` still resolves (no drift).

### B. Emit + gate

- `pnpm adapters:generate` writes the gallery; confirm CLI bundle bytes
  unchanged (git diff shows only gallery.md added + the 4 generated .ts
  untouched).
- `pnpm adapters:gate` green (both bundle + gallery).

### C. VitePress + docs/CLAUDE.md

- `config.ts`: nav "Adapters" → `/adapters/gallery`; sidebar `/adapters/`
  group gets `{ text: "Gallery", link: "/adapters/gallery" }` at the TOP.
- `docs/CLAUDE.md`: add gallery.md to the auto-generated list + map row.

### D. Five example READMEs → one template

Template (≤100 lines, hand-authored):
- `# chartlang-example-<id>-adapter`
- one-line purpose
- badge row: license, "copy-only — not published", render tech,
  "full conformance" (plain `experimental` stability label kept for the
  readme-check gate on canvas2d; use a single stability label word)
- `## Get it`: `npx @invinite-org/chartlang-cli add-adapter <id>` + GitHub
  folder link + "Not published" line (canvas2d needs it for the gate)
- `## Public surface`: createXAdapter, X_CAPABILITIES, default export
- `## How drawings render`: one line — shared `decomposeDrawing` →
  ctx/nodes/graphic
- ```ts minimum-viable call (keep existing, trimmed)
- `## Docs`: gallery + `/adapters/reference/<id>`
- `## License`: MIT
Keep library-specific content but consistent structure. canvas2d must keep
the readme-check-required pieces.

### E. Root README + first-adapter guide

- Root: replace the Task-13 "Five rendering adapters" table block with an
  "Adapters" subsection: one-command `add-adapter` line + gallery link.
  Keep ≤300 lines.
- `write-your-first-adapter.md`: add `add-adapter <library>` fast path next
  to scaffold + gallery link.

### F. Formatting fix (KNOWN ISSUE)

- Run `pnpm format` / biome on the five `examples/*-adapter/` dirs to clear
  the ~12 package.json/tsconfig.json multi-line-array failures from Wave 5.
- Confirm `pnpm format:check` clean. If it touches anything beyond
  examples/* package.json/tsconfig/README → STOP + report.

## Gates to run
- `pnpm adapters:generate` then `pnpm adapters:gate`
- `pnpm typecheck`, `pnpm lint`, `pnpm format:check`
- `pnpm test:scripts` (generator test)
- `pnpm docs:check`, `pnpm docs:build` (no dead links)
- `pnpm readme:check`

## Changeset
None (docs + generator + private-example READMEs; cli minor landed in
Task 14). Add docs-only patch changeset ONLY if a gate strictly requires it.
