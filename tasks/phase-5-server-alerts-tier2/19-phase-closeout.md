# Task 19 — Phase closeout: docs sweep, version bumps, changeset bundle

> **Status: TODO**

## Goal

Close Phase 5 cleanly. Re-run `pnpm chartlang docs` to regenerate
every primitive's auto-generated docs page, sweep every README for
the ≤ 100 line cap, bump every affected package's
`package.json.version` to `0.5.x` via the bundled changeset, verify
every done-criterion from the Phase-5 README, and update the
README's projected `STATEFUL_PRIMITIVES.size` to the actual final
value (171 — Task 18 noted the off-by-one).

## Prerequisites

- Task 18: `ta.fixedRangeVolumeProfile` shipped. Every Phase-5
  capability landed.

## Current Behavior

- Each per-task changeset is committed but not yet released.
- Auto-generated docs pages are stale from intermediate tasks.
- Some package READMEs may have grown past the 100-line cap during
  Phase-5 surface additions.

## Desired Behavior

- `pnpm chartlang docs` regenerates every doc page; `pnpm docs:check`
  green.
- `pnpm readme:check` green on every package; root README ≤ 300
  lines; every package README ≤ 100 lines.
- Every Phase-5 changeset rolls up to a `minor` bump of every
  affected package version to `0.5.0` (or `0.5.x` per the version-
  bump strategy each changeset declares).
- `pnpm changeset version` applied; `pnpm changeset publish` rehearsal
  via `--dry-run` succeeds.
- The Phase-5 README's projection (170) updated to the actual final
  cardinality (171).
- Every done-criterion bullet ticked.

## Requirements

### 1. Docs sweep

- `pnpm chartlang docs` — regenerates `docs/primitives/` for every
  primitive added in Phase 5.
- `pnpm docs:check` — passes with no diffs.
- Audit `docs/` for stale entries that reference dropped surfaces.
  (Phase 5 is purely additive; no drops expected — confirm.)

### 2. README sweep

- Run `pnpm readme:check` and fix any package README that grew past
  100 lines during Phase 5 (likely candidates: `host-quickjs`,
  `host-worker` post-IDB stanza, `runtime` post-VP additions).
- Root `README.md` stays ≤ 300 lines.
- Update `packages/host-quickjs/README.md` to drop any
  "experimental" caveats that were tightened by Task 8's sandbox
  suite (the stability label stays `experimental` until 1.0, but
  feature listings should reflect what shipped).

### 3. Phase-5 README cross-check

- Confirm `tasks/phase-5-server-alerts-tier2/README.md` "Target State"
  section's `STATEFUL_PRIMITIVES` final cardinality (171) matches
  the actual test assertion in
  `packages/core/src/statefulPrimitives.test.ts`. If they diverge —
  audit each task's increment and reconcile.

### 4. Version bumps

Run `pnpm changeset version`:

- Every package touched in Phase 5 bumps to `0.5.x`:
  - `@invinite-org/chartlang-core`
  - `@invinite-org/chartlang-compiler`
  - `@invinite-org/chartlang-runtime`
  - `@invinite-org/chartlang-adapter-kit`
  - `@invinite-org/chartlang-host-worker`
  - `@invinite-org/chartlang-host-quickjs`
  - `@invinite-org/chartlang-conformance`
  - The canvas2d-adapter (`examples/canvas2d-adapter/package.json`)
- Each `PACKAGE_VERSION` constant in `src/index.ts` matches the
  bumped value (Phase 0 convention; pinned by `scripts/version-sync.ts`
  if it exists, else by manual sync).

### 5. Changeset rehearsal

- `pnpm changeset publish --dry-run` — confirms every bumped
  package would publish cleanly. Resolve any flagged dep-cycle or
  unwritten changeset.

### 6. Phase-5 done-criteria verification

Walk every bullet in the Phase-5 README's "Phase 5 closes when:"
section and confirm:

- [ ] Every task's `Acceptance Criteria` checked off in its task
      file. (Manual: visit each `1-…md` through `18-…md` and
      confirm.)
- [ ] `pnpm -r test` — green; coverage at 100% on every Phase-5-
      affected package.
- [ ] `pnpm conformance` — green; new scenarios (MTF ×3,
      defineAlertCondition ×3, runtime.log ×4, draw.table ×2,
      PlotKinds ×16, VPs ×6) all pass.
- [ ] Cross-host determinism test green
      (`packages/host-quickjs/src/integration.test.ts`).
- [ ] Warm-start determinism test green
      (`packages/runtime/src/persistentStateStore.determinism.test.ts`).
- [ ] `pnpm docs:check` green.
- [ ] `pnpm readme:check` green.
- [ ] `pnpm changeset status` shows every package at `0.5.x`.
- [ ] `host-quickjs` `roundTrip` bench ≤ 100× host-worker baseline.

### 7. CHANGELOG normalisation

Each package's `CHANGELOG.md` (generated by `pnpm changeset version`)
gets a manual review pass to ensure the Phase-5 entries are coherent.
Specifically:

- Group the Phase-5 changesets under a "Phase 5" header in each
  package's changelog.
- Cross-reference task numbers in changelog entries where helpful.

### 8. Phase-6 hand-off note

Append a "Phase 6 ready" note at the bottom of the Phase-5 README's
"Deferred / Follow-Up Work" section pointing at the Phase-6 task
folder (`tasks/phase-6-tier3-ltf/`). Just a one-line link.

### 9. Final cross-cutting tests

- `pnpm -r build` — confirms every package builds clean post-version
  bump (`PACKAGE_VERSION` syncs picked up).
- `pnpm -r typecheck` — green across the workspace.
- `pnpm -r lint` — green.
- `pnpm bench:ci` — every bench under threshold.

## Files to Create / Modify

| File | Action | Purpose |
|------|--------|---------|
| `tasks/phase-5-server-alerts-tier2/README.md` | Modify | Correct 170 → 171; add Phase-6 link |
| `packages/*/README.md` | Modify | Trim to ≤ 100 lines where needed |
| `packages/*/package.json` (bumped versions) | Modify | Via `pnpm changeset version` |
| `packages/*/src/index.ts` (PACKAGE_VERSION) | Modify | Sync to bumped version |
| `packages/*/CHANGELOG.md` | Modify | Generated + reviewed |
| `docs/primitives/**/*.md` | Modify | Regenerated by `pnpm chartlang docs` |

## Gates

- `pnpm typecheck`
- `pnpm lint`
- `pnpm -r test` (100% coverage)
- `pnpm docs:check`
- `pnpm readme:check`
- `pnpm conformance`
- `pnpm bench:ci`
- `pnpm changeset publish --dry-run`

## Changeset

No new per-task changeset. This task lands the **release** of all
prior Phase-5 changesets via `pnpm changeset version`.

## Acceptance Criteria

- [ ] All 18 prior Phase-5 changesets applied; affected packages at
      `0.5.x`.
- [ ] `pnpm changeset publish --dry-run` exits 0.
- [ ] `pnpm chartlang docs` regenerated; `pnpm docs:check` green.
- [ ] `pnpm readme:check` green.
- [ ] Phase-5 README's cardinality projection corrected (170 → 171).
- [ ] Every done-criterion bullet from the Phase-5 README ticked.
- [ ] `pnpm -r test` green at 100% coverage.
- [ ] `pnpm conformance` green; every new scenario passing.
- [ ] `host-quickjs` bench gates green.
- [ ] Phase-6 hand-off link added.
