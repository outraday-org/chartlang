# Auth token for publishing is NOT stored here — see .github/CLAUDE.md §22.11.
# Manual release uses `npm login` (writes to ~/.npmrc). When CI release is
# re-enabled, the workflow writes the auth line at publish time.
@invinite-org:registry=https://registry.npmjs.org/
access=public
