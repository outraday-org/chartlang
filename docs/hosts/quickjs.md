# QuickJS host

> **Phase:** Lands in Phase 5.
> **Cross-reference:** See PLAN.md §8.3.

`@invinite-org/chartlang-host-quickjs`, the WASM-isolated host for
untrusted or server-side execution: runs a compiled artifact inside a
QuickJS-WASM membrane with hard CPU / memory limits, used by alert
back-ends and CI conformance runs.

Stubbed during the Phase 0 bootstrap so the docs gate has a stable
target. Content lands with the Phase 5 server-side-alerts PR.
