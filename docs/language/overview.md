# Language overview

> **Phase:** Lands in Phase 1.
> **Cross-reference:** See PLAN.md §4.

The narrative tour of the chartlang eDSL: what a `.chart.ts` file looks
like, the `defineIndicator` shape, the `compute` per-bar contract, the
`Series<T>` model, and the module surface (`ta.*`, `plot.*`, `draw.*`,
`alert.*`, `input.*`, `color.*`, `timeframe.*`, `barstate.*`,
`syminfo.*`, `state.*`).

Stubbed during the Phase 0 bootstrap so the docs gate has a stable
target. Content lands with the Phase 1 core-types PR.
