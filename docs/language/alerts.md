# Alerts

> **Phase:** Lands in Phase 1/5.
> **Cross-reference:** See PLAN.md §11.

The alert surface: `alert()` immediate-fire calls, `defineAlertCondition`
for user-wired alerts, the alert payload schema, and the
`runtime.log.*` debugging channel that ships alongside.

Stubbed during the Phase 0 bootstrap so the docs gate has a stable
target. Initial content lands with Phase 1's `alert()`; Phase 5
expands to `defineAlertCondition` and the QuickJS server-side path.
