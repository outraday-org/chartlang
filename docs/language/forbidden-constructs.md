# Forbidden constructs

The canonical forbidden-construct list lives in the
[grammar and static analyses spec](../spec/grammar.md#forbidden-constructs).
This language page stays intentionally short so the diagnostic table has a
single source of truth.
