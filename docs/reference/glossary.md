# Glossary

> **Phase:** Lands in Phase 1+ (cross-cuts every phase).
> **Cross-reference:** Cross-cuts PLAN.md §3–§14.

Definitions for every load-bearing term in chartlang: `Series<T>`,
`Adapter`, `Capabilities`, `ScriptHost`, `apiVersion`, `Manifest`,
`DrawingKind`, `compute`, warmup, NaN-correctness, conformance, plus
the role of each package in `packages/*`.

Stubbed during the Phase 0 bootstrap so the docs gate has a stable
target. Content grows as each phase introduces new terms.
