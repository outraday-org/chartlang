<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `draw.threeDrivesPattern`

> **Stability:** experimental
> **Since:** 0.3
> **Bucket:** `polylines`
> **Wire kind:** `three-drives-pattern`

Draw a three-drives reversal pattern through 7 world anchors
`[start, drive1, retr1, drive2, retr2, drive3, end]`. The renderer
strokes the 6-leg open polyline and labels each pivot. Mirrors
invinite's `three-drives-pattern-tool.ts` shape.

## Anchors

`anchors` — `[start, drive1, retr1, drive2, retr2, drive3, end]`

Anchor count: 7.

## Signature

```ts
function threeDrivesPattern(anchors: AnchorHept, opts?: LineDrawStyle): DrawingHandle;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `draw.threeDrivesPattern(...)` without it._

## Example

```ts
import { defineIndicator } from "@invinite-org/chartlang-core";
    export default defineIndicator({
        name: "draw.threeDrivesPattern demo",
        apiVersion: 1,
        compute({ bar, draw }) {
            draw.threeDrivesPattern([
                { time: bar.time, price: bar.low },
                { time: bar.time + 10_000, price: bar.high },
                { time: bar.time + 20_000, price: bar.close },
                { time: bar.time + 30_000, price: bar.high + 1 },
                { time: bar.time + 40_000, price: bar.close + 0.5 },
                { time: bar.time + 50_000, price: bar.high + 2 },
                { time: bar.time + 60_000, price: bar.close + 1 },
            ]);
        },
    });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/emit/draw/patterns/threeDrivesPattern.ts)
- [`draw.*` namespace index](./index.md)
- [PLAN §10 — Drawing primitives](../../../PLAN.md#10-drawing-primitives)
