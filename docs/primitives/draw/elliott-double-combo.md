<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `draw.elliottDoubleCombo`

> **Stability:** stable
> **Since:** 0.3
> **Bucket:** `polylines`
> **Wire kind:** `elliott-double-combo`

Draw an Elliott seven-anchor W-X-Y double-three corrective pattern
through 7 world anchors `[start, W-end, x1, X-end, x2, Y-mid,
Y-end]`. The renderer strokes the 6-leg connecting polyline and
labels each pivot. Pass `opts.labels` to override the default
`["S", "W", "x1", "X", "x2", "Yi", "Y"]` labels.

## Anchors

`anchors` — `[start, W-end, x1, X-end, x2, Y-mid, Y-end]`

Anchor count: 7.

## Signature

```ts
function elliottDoubleCombo(anchors: AnchorHept, opts?: LineDrawStyle): DrawingHandle;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `draw.elliottDoubleCombo(...)` without it._

## Example

```ts
import { defineIndicator } from "@invinite-org/chartlang-core";
    export default defineIndicator({
        name: "draw.elliottDoubleCombo demo",
        apiVersion: 1,
        compute({ bar, draw }) {
            draw.elliottDoubleCombo([
                { time: bar.time, price: bar.low },
                { time: bar.time + 10_000, price: bar.high },
                { time: bar.time + 20_000, price: bar.close },
                { time: bar.time + 30_000, price: bar.high + 1 },
                { time: bar.time + 40_000, price: bar.close - 0.5 },
                { time: bar.time + 50_000, price: bar.high + 2 },
                { time: bar.time + 60_000, price: bar.close + 1 },
            ]);
        },
    });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/emit/draw/elliott/elliottDoubleCombo.ts)
- [`draw.*` namespace index](./index.md)
- [PLAN §10 — Drawing primitives](../../../PLAN.md#10-drawing-primitives)
