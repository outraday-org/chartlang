<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `draw.doubleCurve`

> **Stability:** stable
> **Since:** 0.3
> **Bucket:** `polylines`
> **Wire kind:** `double-curve`

Draw a cubic Bezier curve through five world anchors `[P0, P1, mid,
P3, P4]`. The renderer paints a single cubic from `P0` to `P4` with
off-curve controls `P1` and `P3`; the middle anchor `mid` is the
stitch point for future split-rendering and is currently preserved
but unused at paint time. Mirrors invinite's `double-curve-tool.ts`
persisted shape.

## Anchors

`anchors` — `[P0, P1, mid, P3, P4]` quint

Anchor count: 5.

## Signature

```ts
function doubleCurve(anchors: AnchorQuint, opts?: LineDrawStyle): DrawingHandle;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `draw.doubleCurve(...)` without it._

## Example

```ts
import { defineIndicator } from "@invinite-org/chartlang-core";
    export default defineIndicator({
        name: "draw.doubleCurve demo",
        apiVersion: 1,
        compute({ bar, draw }) {
            draw.doubleCurve(
                [
                    { time: bar.time, price: bar.open },
                    { time: bar.time, price: bar.high },
                    { time: bar.time, price: (bar.high + bar.low) / 2 },
                    { time: bar.time, price: bar.low },
                    { time: bar.time, price: bar.close },
                ],
                { color: "#a855f7", lineWidth: 2 },
            );
        },
    });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/emit/draw/curves/doubleCurve.ts)
- [`draw.*` namespace index](./index.md)
