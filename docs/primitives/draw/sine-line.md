<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `draw.sineLine`

> **Stability:** experimental
> **Since:** 0.3
> **Bucket:** `other`
> **Wire kind:** `sine-line`

Draw a sinusoidal projection fitted between two world anchors. The
baseline is the midpoint price of the two anchors; the amplitude is
half the price span; the half-period spans `|to.time - from.time|`
(so the full period is twice that). The renderer samples 32 points
per full period and extends the wave across the visible viewport.

## Anchors

`a`, `b` — two `WorldPoint`s `(from, to)`

Anchor count: 2.

## Signature

```ts
function sineLine(a: WorldPoint, b: WorldPoint, opts?: LineDrawStyle): DrawingHandle;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `draw.sineLine(...)` without it._

## Example

```ts
import { defineIndicator } from "@invinite-org/chartlang-core";
    export default defineIndicator({
        name: "draw.sineLine demo",
        apiVersion: 1,
        compute({ bar, draw }) {
            draw.sineLine(
                { time: bar.time, price: bar.low },
                { time: bar.time + 60_000, price: bar.high },
                { color: "#0ea5e9" },
            );
        },
    });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/emit/draw/cycles/sineLine.ts)
- [`draw.*` namespace index](./index.md)
- [PLAN §10 — Drawing primitives](../../../PLAN.md#10-drawing-primitives)
