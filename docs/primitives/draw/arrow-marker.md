<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `draw.arrowMarker`

> **Stability:** stable
> **Since:** 0.3
> **Bucket:** `labels`
> **Wire kind:** `arrow-marker`

Draw a compact arrow-marker glyph at a single world anchor. The
canvas2d renderer paints a small filled dot at the anchor, a short
stub line, and an arrowhead — a self-contained "annotation lives
here" marker. Optional `style.text` paints next to the dot. Default
color is `"#3b82f6"` (invinite toolbar blue). Mirrors invinite's
`arrow-marker-tool.ts` shape.

## Anchors

`anchor` — single `WorldPoint`

Anchor count: 1.

## Signature

```ts
function arrowMarker(anchor: WorldPoint, opts?: ArrowMarkerOpts): DrawingHandle;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `draw.arrowMarker(...)` without it._

## Example

```ts
import { defineIndicator } from "@invinite-org/chartlang-core";
    export default defineIndicator({
        name: "draw.arrowMarker demo",
        apiVersion: 1,
        compute({ bar, draw }) {
            draw.arrowMarker(
                { time: bar.time, price: bar.close },
                { color: "#10b981", text: "Long" },
            );
        },
    });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/emit/draw/annotations/arrowMarker.ts)
- [`draw.*` namespace index](./index.md)
- [PLAN §10 — Drawing primitives](../../../PLAN.md#10-drawing-primitives)
