<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `ta.vol`

> **Stability:** experimental
> **Since:** 0.2

Volume passthrough — emits `bar.volume` as a `Series<number>` so
script authors can compose it like any other primitive (e.g. plot
as a histogram). Stateless math; the slot exists purely to back
the cached `Series<T>` Proxy + the per-callsite output buffer the
compiler-injected slot id keys.

Tick-mode reads the live `bar.volume`, replacing the head — volume
can change mid-bar in real time.

## Formula

out[t] = bar.volume[t]

## Warmup

0

## Signature

```ts
function vol(slotId: string, _opts?: VolOpts): Series<number>;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `ta.<id>(...)` without it._

## Parameters

| Name | Type | Default | Description |
|---|---|---|---|
| `slotId` | `string` | — | — |
| `_opts` | `VolOpts` | (optional) | — |

## Returns

`Series<number>`

## Example

```ts
// import { ta, plot } from "@invinite-org/chartlang-runtime";
    // const v = ta.vol("slot");
    // plot(v, { style: { kind: "histogram", baseline: 0 } });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/ta/vol.ts)
- [Reference math](../../../PLAN.md#92-full-primitive-list)
