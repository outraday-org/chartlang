<!-- AUTO-GENERATED by chartlang docs — do not edit by hand -->

# `ta.anchoredVolumeProfile`

> **Stability:** stable
> **Since:** 0.5

Anchored Volume Profile — reads a user-picked time anchor and
bucketizes volume from that anchor forward.

## Formula

Port of invinite anchored-volume-profile: find the first
candle with `time >= anchor`, bucket anchor→right-edge
volume by price, then derive POC / value-area high /
value-area low through the shared volume-profile pipeline.

## Warmup

NaN through the bar at `anchor`; first bar after `anchor`
can emit once the anchor→current window has positive volume.

## Anchors

`opts.anchor: Time` — supplied by `input.time(..., { pickFromChart: true })`.

## Signature

```ts
function anchoredVolumeProfile(slotId: string, opts: AnchoredVolumeProfileOpts): AnchoredVolumeProfileResult;
```

_The leading `slotId: string` parameter is injected by the chartlang compiler at every callsite — script authors call `ta.<id>(...)` without it._

## Parameters

| Name | Type | Default | Description |
|---|---|---|---|
| `slotId` | `string` | — | — |
| `opts` | `AnchoredVolumeProfileOpts` | — | — |

## Returns

`AnchoredVolumeProfileResult`

## Example

```ts
// import { input, plot, ta } from "@invinite-org/chartlang-core";
    // const anchor = input.time(0, { pickFromChart: true });
    // const vp = ta.anchoredVolumeProfile({ anchor: anchor.value, rowSize: 24 });
    // plot(vp.poc, { style: { kind: "horizontal-histogram", buckets: vp.buckets } });
```

## See also

- [Source on GitHub](https://github.com/outraday-org/chartlang/blob/main/packages/runtime/src/ta/anchoredVolumeProfile.ts)
